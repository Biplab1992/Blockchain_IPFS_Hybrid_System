{
  "name": "blockchain-ipfs-hybrid-system",
  "private": true,
  "version": "1.0.0",
  "description": "Monorepo scripts for app, backend, and contracts",
  "scripts": {
    "dev": "concurrently \"npm --prefix backend run dev\" \"npm --prefix app run dev\"",
    "test": "npm --prefix contracts run test",
    "lint": "npm --prefix backend exec -- tsc --noEmit -p backend/tsconfig.json && npm --prefix contracts run test && npm --prefix app run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
